module.exports = {
    presets: ['@babel/preset-env', '@babel/preset-react'],
    ignore: [
      '**/*.svg', // Exclude all SVG files
      // You can add more patterns to exclude other large files if needed
    ],
  };